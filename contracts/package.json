{
  "name": "contracts",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy": "hardhat run scripts/deploy.js --network privatenet",
    "populate": "hardhat run scripts/populate.js --network privatenet"
  },
  "devDependencies": {
    "hardhat": "^2.22.17",
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "dotenv": "^16.4.5",
    "prompts": "^2.4.2"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.1.0"
  }
}
